# CCNA用語集

VitePressで作るCCNA用語集サイトです。

## 開発

```sh
npm install
npm run docs:dev
```

## ビルド

```sh
npm run docs:build
```

## GitHub Pages

GitHubのリポジトリ設定で Pages の Source を `GitHub Actions` にすると、`main` ブランチへのpushで自動デプロイされます。

公開URLは通常、次の形式です。

```text
https://<githubユーザー名>.github.io/ccna/
```
